plugins {
    id 'java-library'
    id 'maven-publish'
}

dependencies {
}

/*
publishing {
    publications {
        maven(MavenPublication) {
            groupId = project.group
            version = project.version
            from components.java
        }
    }
}*/

